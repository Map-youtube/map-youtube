<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Map with YouTube Videos</title>
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAjMNd7u5X5jetqbFXJEr-hF-Ku8_oReGI"></script>
    <script src="https://cdn.jsdelivr.net/npm/papaparse@5.3.0/papaparse.min.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
        }
        #map {
            width: 100%;
            margin-top: 10vh;
        }

        /* PC 환경 */
        @media (min-width: 768px) {
            #map {
                height: 70vh; /* PC 환경에서 지도 높이 설정 */
            }
        }
    </style>
</head>
<body>
    <div id="map"></div>

    <script>
        // 구글 맵 초기 설정
        let currentInfoWindow;  // 현재 열려 있는 정보창을 저장할 변수

        function initMap() {
            const map = new google.maps.Map(document.getElementById("map"), {
                center: { lat: 15, lng: 150 },
                zoom: 3,  // 전 세계 지도가 보이도록 줌 레벨 설정
            });

            // CSV 파일에서 데이터 불러오기
            Papa.parse("https://raw.githubusercontent.com/Map-youtube/map-youtube/main/location.csv", {
                download: true,
                header: true,
                complete: function(results) {
                    const data = results.data;
                    data.forEach(location => {
                        const lat = parseFloat(location.latitude);
                        const lng = parseFloat(location.longitude);
                        const name = location.Location;
                        const videoID = location['YouTube Link'];

                        // 유튜브 embed URL 생성
                        const embedURL = `https://www.youtube.com/embed/${videoID}?rel=0&showinfo=0&autoplay=1&mute=1`;

                        // 마커 생성
                        const marker = new google.maps.Marker({
                            position: { lat, lng },
                            map,
                            title: name
                        });

                        // 유튜브 영상이 포함된 정보창 설정
                        const infoWindow = new google.maps.InfoWindow({
                            content: `
                                <div class="info-window">
                                    <h3>${name}</h3>
                                    <iframe width="100%" height="300" src="${embedURL}" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
                                </div>
                            `
                        });

                        // 마커 클릭 시 기존 정보창 닫고 새 정보창 열기
                        marker.addListener("click", () => {
                            if (currentInfoWindow) {
                                currentInfoWindow.close();
                            }
                            infoWindow.open(map, marker);
                            currentInfoWindow = infoWindow;
                        });
                    });
                }
            });
        }

        // 모바일 환경에서 지도의 높이를 동적으로 설정
        function adjustMapHeight() {
            const mapElement = document.getElementById("map");
            if (window.innerWidth < 768) {  // 모바일 환경 판단
                mapElement.style.height = "20vh";  // 모바일에서 50vh 높이로 설정
            }
        }

        // 페이지 로드 및 리사이즈 시 높이 조절
        window.onload = () => {
            initMap();
            adjustMapHeight();
        };
        window.onresize = adjustMapHeight;
    </script>
</body>
</html>
